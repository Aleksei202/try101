def compute_patterns(pattern='new pattern'):
    inputs = [pattern]
    patterns = ['a list based on'] + inputs

    return patterns


print(compute_patterns())
print(compute_patterns())
print(compute_patterns('1'))
print(compute_patterns('2'))
